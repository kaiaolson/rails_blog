<h1>All Posts (<%= @posts.count %>)</h1>

<% @posts.order("id").each do |post| %>
  <div>
     <h3><%= link_to post.title, post_path(post) %></h3>
     <hr>
  </div>
<% end %>

<% if @posts.count > 10 %>
<%= will_paginate @posts %>
<% else %>
<% end %>
